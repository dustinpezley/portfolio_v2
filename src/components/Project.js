import React from 'react';

const Project = () => {
  const projects = [
    {
      /* Need to populate an array of project objects with image references */
    },
  ];

  return (
    <div id="projects">
      <h1 id="projects-title">Projects</h1>
      <div id="projects-container"></div>
    </div>
  );
};

export default Project;
